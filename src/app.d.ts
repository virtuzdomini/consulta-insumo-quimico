// Tipos globais do SvelteKit. Deixe as interfaces vazias — o SvelteKit
// as usa para você tipar `locals`, `PageData`, `Error`, etc. quando precisar.
// See https://svelte.dev/docs/kit/types#app.d.ts

declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
