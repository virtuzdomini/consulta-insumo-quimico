<script lang="ts">
	// Selo "Identificado" no canto superior direito do cartão — confirma
	// visualmente que o composto foi encontrado na base.
</script>

<span class="selo">Identificado</span>

<style>
	.selo {
		font:
			700 11px var(--fonte-ui);
		color: var(--cor-acento);
		background: var(--cor-acento-suave);
		border: 1px solid var(--cor-acento-borda);
		border-radius: var(--raio-pilula);
		padding: 6px 13px;
		letter-spacing: 0.03em;
		white-space: nowrap;
	}
</style>
