# Configuração de deploy na Netlify.
# O adapter-netlify já gera as functions; aqui só apontamos o comando de build.
[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"
